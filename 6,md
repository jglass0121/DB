기본키(Primary Key)는 **테이블에서 각 행(row)을 유일하게 식별하기 위해 설정하는 컬럼 또는 컬럼들의 집합**이야. 가장 중요한 특징은 아래 2가지!

1/ 데이터 무결성 보장 : 데이터가 정확하고, 일관되면 

✔️ 각 행(레코드)을 유일하게 식별할 수 있는 속성(컬럼)
예시
● 사용자 → 주민등록번호(001231-3000000)
● 제품 → EAN(8801234567890)

## ✅ 기본키(Primary Key)의 특징

1. **유일성(Unique)**
    - 기본키 값은 테이블 안에서 **중복될 수 없어**.
    - 예: 회원번호 1번이 두 번 나오면 안 됨.
2. **NOT NULL**
    - 기본키 컬럼은 반드시 값이 있어야 해. **NULL이 될 수 없어**.

## 🚫 기본키로 사용할 수 없는 것

- 중복될 수 있는 값
- NULL이 될 수 있는 값
- 자주 바뀌는 값(전화번호, 이메일 등은 비추천)

## 📌 기본키 설정 방식

- **단일 기본키(Single PK)**: 하나의 컬럼만 사용
    - 예: id, student_id
- **복합 기본키(Composite PK)**: 여러 개의 컬럼을 묶어 기본키로 사용
    - 예: (학생번호, 과목코드) 같이 두 컬럼 조합이 유일해야 하는 경우

기본키 설계가 중요한 이유?

1. 데이터 무결성(Data Integrity) 보장
기본키는 테이블의 **각 레코드를 유일하게 식별**해야 하므로, 동일한 값이 두 번 입력될 수 없고, 이를 통해 **중복된 데이터가 저장되는 것을 방지하여**, 데이터의 정확성과 신뢰성 확보
2. 인덱싱(Indexing)과 빠른 검색
기본키는 **자동으로 인덱스가** 생성되며, 이를 기반으로 검색할 경우 필요한 데이터를 빠르게 찾아 **효율적인 검색이** 가능
3. 참조 무결성(Referential Integrity) 유지
관계형 데이터베이스에서는 다른 테이블의 기본키를 외래키로 참조함으로써 **테이블 간의 관계를 정의하게** 되는데, 정확하게 설계된 기본키가 있어야만 이러한 참조 관계가 일관성과 무결성을 유지하면서 동작
4. 효율적인 저장 공간 활용
기본키는 다른 테이블이 외래키로 참조할 경우 해당 값이 여러 테이블에 저장되는데, 너무 길거나 복합적인 기본키(예: 여러 컬럼 조합)는 이를 참조하는 외래키에서도 많은 저장 공간을 차지

기본키 선정의 기준

1. 고유성(Uniqueness) → 중복된 값이 없어야 한다
2. 불변성(Immutability) → 변경되지 않는 값이어야 한다
3. 간결성(Simplicity) → 가능한 짧고 단순해야 한다

![image.png](attachment:90cc6c24-96da-4a3a-a834-4cd2f89620b1:image.png)

## 자연키(Natural Key)

실제 현실 세계의 속성 중에서 본래부터 고유하게 식별할 수 있는 값을 기본키로 사용하는 것

예시

- 주민등록번호
- 차량 번호판
- ISBN(도서 고유번호)

자연키는 **의미 있는 값을 사용해 직관적이지만**, 변경 가능성, 개인정보 노출 위험, 키의 복잡성 및 길이 등의 이유로 실무에서는 보통 **기본키로 사용하지 않는다**

## 대체키(Surrogate Key)

인위적으로 생성된, 의미 없는 고유 식별자
대표적으로 숫자형 자동 증가값(INT AUTO_INCREMENT)

![image.png](attachment:a8576135-12e4-4587-b9b4-3e5f54c12be7:image.png)

**숫자형 자동 증가값 특징**

숫자형 자동 증가값은 단일 데이터베이스에서 자동으로 할당해주는 순차적인 숫자이기 때문에, 사용이 간편하고 중복 없이 고유한 식별자를 생성한다

하지만 분산 환경에서는 충돌 위험이 있고, 삭제로 인한 번호 간격이 발생하며, 보안상 예측 가능하다

---